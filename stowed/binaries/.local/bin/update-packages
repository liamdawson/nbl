#!/usr/bin/env bash

set -eu

command_exists() {
  command -v "$1" >/dev/null 2>&1
}

is_macos() {
  test "$(uname)" = 'Darwin'
}

! command_exists apt-get || (sudo apt-get update && sudo apt-get upgrade -y)
! command_exists brew || brew upgrade
! is_macos || brew cask upgrade
